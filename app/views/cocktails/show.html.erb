<br>
<br>
<br>
<div class="container-fluid">
  <div> <h1><%= @cocktail.name %></h1></div>
  <div class="head">
    <div>
    <%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %></div>
    <div>
    <%= link_to '+Add a new dose', new_cocktail_dose_path(@cocktail) %>
    </div>
  </div>
  <br>
  <br>

  <ul>
    <% @cocktail.doses.each do |dose| %>
      <li> <%= dose.description %> <%= dose.ingredient.name %>  - <%= link_to 'Destroy', dose, method: :delete %>
      </li>
    <% end %>
  </ul>

<%= link_to 'Back to cocktails list', cocktails_path %>
</div>
